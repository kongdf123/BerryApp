<UserControl x:Class="BerryApp.WPF.Pages.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BerryApp.WPF.Pages"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
        </Grid.RowDefinitions>
        
        <!--Title-->
        <TextBlock Text="Orders Management" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"></TextBlock>

        <!--Input-->
        <StackPanel Orientation="Horizontal" Margin="0,40,0,10">
            <Label Content="Product Name:"></Label>
            <TextBox Width="200" Margin="5,0,20,0" Text="{Binding NewProductName}"></TextBox>
            <Label Content="Quantity:"></Label>
            <TextBox Width="100" Margin="5,0,20,0" Text="{Binding NewQuantity}"></TextBox>
            <Button Content="Add Order" Command="{Binding AddCommand}"></Button>
        </StackPanel>

        <!--DataGrid-->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Orders}" SelectedItem="{Binding SelectedOrder}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"></DataGridTextColumn>
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*"></DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!--Actions-->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Delete" Command="{Binding DeleteCommand}"></Button>
        </StackPanel>
    </Grid>
</UserControl>
